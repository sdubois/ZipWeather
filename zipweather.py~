from flask import Flask
import pywapi
import pprint
pp = pprint.PrettyPrinter(indent=4)
result = pywapi.get_weather_from_google('14623')

app = Flask(__name__)

@app.route('/')
def index():
    #pp.pprint(result)
    return " d"

@app.route('/<zipcode>/')
def show_zipcode(zipcode):
    return zipcode

if __name__ == '__main__':
    app.debug = True
    app.run()
